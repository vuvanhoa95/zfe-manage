# 10. Deploy Fast

Deploy production bỏ qua build test local (nhanh hơn).

Deploy production bỏ qua build test local (nhanh hơn).

```powershell
powershell -ExecutionPolicy Bypass -File ./deploy-production.ps1 --skip-build
```
